import { GetCardSpacingPipe } from './get-card-spacing.pipe';

describe('GetCardSpacingPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCardSpacingPipe();
    expect(pipe).toBeTruthy();
  });
});
