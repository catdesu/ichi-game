import { PlayerTurnStylePipe } from './player-turn-style.pipe';

describe('PlayerTurnStylePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerTurnStylePipe();
    expect(pipe).toBeTruthy();
  });
});
