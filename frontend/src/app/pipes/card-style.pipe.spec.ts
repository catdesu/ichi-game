import { CardStylePipe } from './card-style.pipe';

describe('CardStylePipe', () => {
  it('create an instance', () => {
    const pipe = new CardStylePipe();
    expect(pipe).toBeTruthy();
  });
});
